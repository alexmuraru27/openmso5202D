import "./styles.css";

// The frontend is intentionally NOT wired to the backend yet. A dedicated backend-API
// layer will expose the scope to the UI later; until then this is a static shell with no
// `invoke` calls into the Rust side.
console.info("openmso5202D frontend — backend API not yet connected");
